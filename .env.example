# Winner-Grounded Copy Engine — environment
#
# Copy to .env.local for local dev. On the VPS these live in the
# systemd unit's EnvironmentFile, owned by the isolated app user (600).
# NEVER commit real keys.

# Anthropic — generation + screenshot vision. Create a FRESH, spend-capped key.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# Firecrawl — URL scrape, VSL transcript, Meta Ad Library pull.
FIRECRAWL_API_KEY=fc-xxxxxxxx
